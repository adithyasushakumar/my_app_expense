<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ae.bluecast.my_app_expense">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Login_Activity" />
        <activity android:name=".activities.Register_Activity" />
        <activity android:name=".activities.Mainpage_Activity" />
        <activity android:name=".activities.Calandar_Activity" />
        <activity android:name=".activities.Calculator_Activity" />
        <activity android:name=".activities.TodoList" />
        <activity android:name=".activities.Diary" />
        <activity android:name=".activities.AddDiary" />
        <activity android:name=".sample.view.activity.ExpenseDetailsActivity" />
        <activity android:name=".sample.view.activity.ExpenseDateActivity"></activity>
    </application>

</manifest>